#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;


int A[10],N,M;
int check[10];
vector<int>c;

int main()
{
    ios_base::sync_with_stdio(false);

    cin>>N>>M;
    for(int i = 0; i<N; i++){
        cin>>A[i]
    }

    go(0);
    return 0;
}
